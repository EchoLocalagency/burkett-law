# burkett-law

Static site for the Law Office of Brian Burkett (childcustodyanddivorce.com). See `.planning/PROJECT.md` for full context. Deploy via `git push` (Netlify auto-deploys). Never run `netlify deploy` per CLAUDE.md.
